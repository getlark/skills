{
  "description": "Opt-in PostToolUse hook: runs configured getlark workflows after `git commit` or `git push` when .claude/getlark.local.md has `enabled: true`.",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/scripts/validate-branch.sh",
            "timeout": 900
          }
        ]
      }
    ]
  }
}
